<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%= link_to "edit task", edit_task_path(@task)%>
<%= link_to "delete task #{@task.title}",
            destroy_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>